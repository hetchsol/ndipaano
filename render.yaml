# Render Blueprint - Ndipaano Platform
# https://render.com/docs/blueprint-spec

services:
  # NestJS API Server
  - type: web
    name: ndipaano-api
    runtime: node
    region: frankfurt
    plan: starter
    buildCommand: pnpm install && pnpm --filter @ndipaano/shared build && pnpm --filter api build
    startCommand: pnpm --filter api start:prod
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: DATABASE_URL
        fromDatabase:
          name: ndipaano-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          name: ndipaano-redis
          type: redis
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: ENCRYPTION_KEY
        generateValue: true
      - key: WEB_URL
        fromService:
          name: ndipaano-web
          type: web
          property: host
      - key: S3_ENDPOINT
        sync: false
      - key: S3_ACCESS_KEY
        sync: false
      - key: S3_SECRET_KEY
        sync: false
      - key: S3_BUCKET
        value: ndipaano
      - key: S3_REGION
        value: eu-central-1
      - key: PAYSTACK_SECRET_KEY
        sync: false
      - key: PAYSTACK_WEBHOOK_SECRET
        sync: false
      - key: SENDGRID_API_KEY
        sync: false
      - key: AT_API_KEY
        sync: false
    healthCheckPath: /api/health

  # Next.js Web Portal
  - type: web
    name: ndipaano-web
    runtime: node
    region: frankfurt
    plan: starter
    buildCommand: pnpm install && pnpm --filter @ndipaano/shared build && pnpm --filter web build
    startCommand: pnpm --filter web start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_API_URL
        fromService:
          name: ndipaano-api
          type: web
          envVarKey: RENDER_EXTERNAL_URL

databases:
  - name: ndipaano-db
    plan: starter
    databaseName: ndipaano
    postgresMajorVersion: "16"
    ipAllowList: []

  - name: ndipaano-redis
    plan: starter
    type: redis
    ipAllowList: []
